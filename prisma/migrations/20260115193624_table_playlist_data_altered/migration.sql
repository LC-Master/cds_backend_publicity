BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[PlaylistData] ADD [syncStartedAt] DATETIME2,
[syncing] BIT NOT NULL CONSTRAINT [PlaylistData_syncing_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
